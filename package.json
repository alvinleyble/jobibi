{
  "name": "jobibi",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev": "pnpm --filter @jobibi/extension dev",
    "build": "pnpm --filter @jobibi/extension build",
    "compile": "pnpm -r compile",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @jobibi/extension test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
